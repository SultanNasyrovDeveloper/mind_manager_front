import React, { FC } from 'react';
import { Col, Row, Card } from 'ui';

export interface NodeChildrenProps {}

const NodeChildren: FC<NodeChildrenProps> = ({...rest}) => {
  return (
    <div></div>
  );
};

export default NodeChildren;